import { responsiveTable } from '../macro/responsiveTable.js'

/**
 * Generates a menu with the provided index as the currently selected item.
 * @param {number} currentIndex
 * @returns {DomItem}
 */
export const detail = (currentIndex = 1) => {
}
